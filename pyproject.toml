[project]
name = "paperless-mcp"
version = "1.0.3"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.28.1",
]

[project.scripts]
paperless-mcp = "main:main"

[tool.uv]
package = true
